language: cpp
sudo: required
compiler: gcc
before_script:
    - sudo apt-get update
    - sudo apt-get install build-essential
    - sudo apt-get install cmake
    - sudo apt-get install libopencv-dev

script: 
  - cmake .
  - make

