cmake_minimum_required(VERSION 2.8)
project( thai-image-restoration )
find_package( OpenCV REQUIRED )
include_directories( ${OpenCV_INCLUDE_DIRS} )
add_executable( thai-image-restoration src/main.cpp )
target_link_libraries( thai-image-restoration ${OpenCV_LIBS} )